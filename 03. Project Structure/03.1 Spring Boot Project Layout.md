# 03.1 — Spring Boot Project Layout

#spring/structure #java/core

> **Liên kết:** [[00. Java Master MOC]] | [[03.2 Layered Architecture]] | [[04.1 Maven Overview]] | [[04.4 build.gradle Structure]]

---

## 📁 Cấu Trúc Thư Mục Chuẩn

```
my-spring-app/
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── com/example/myapp/
│   │   │       ├── MyAppApplication.java       ← Entry point (@SpringBootApplication)
│   │   │       ├── controller/                 ← HTTP Request Handlers
│   │   │       │   ├── UserController.java
│   │   │       │   └── ProductController.java
│   │   │       ├── service/                    ← Business Logic
│   │   │       │   ├── UserService.java        (interface)
│   │   │       │   └── UserServiceImpl.java    (implementation)
│   │   │       ├── repository/                 ← Data Access Layer
│   │   │       │   └── UserRepository.java
│   │   │       ├── entity/                     ← JPA Entities (DB Tables)
│   │   │       │   └── User.java
│   │   │       ├── dto/                        ← Data Transfer Objects
│   │   │       │   ├── request/
│   │   │       │   │   └── CreateUserRequest.java
│   │   │       │   └── response/
│   │   │       │       └── UserResponse.java
│   │   │       ├── exception/                  ← Custom Exceptions
│   │   │       │   ├── EntityNotFoundException.java
│   │   │       │   └── GlobalExceptionHandler.java
│   │   │       ├── config/                     ← Spring Configuration
│   │   │       │   ├── SecurityConfig.java
│   │   │       │   └── SwaggerConfig.java
│   │   │       └── mapper/                     ← Entity ↔ DTO Conversion
│   │   │           └── UserMapper.java
│   │   └── resources/
│   │       ├── application.yml                 ← App configuration
│   │       ├── application-dev.yml             ← Dev profile config
│   │       ├── application-prod.yml            ← Prod profile config
│   │       └── db/
│   │           └── migration/                  ← Flyway/Liquibase scripts
│   │               ├── V1__create_users_table.sql
│   │               └── V2__add_email_index.sql
│   └── test/
│       └── java/
│           └── com/example/myapp/
│               ├── controller/
│               │   └── UserControllerTest.java
│               ├── service/
│               │   └── UserServiceTest.java
│               └── repository/
│                   └── UserRepositoryTest.java
├── build.gradle  (hoặc pom.xml)
├── gradlew
├── Dockerfile
└── README.md
```

---

## 🚪 Entry Point

```java
package com.example.myapp;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication  // = @Configuration + @EnableAutoConfiguration + @ComponentScan
public class MyAppApplication {
    public static void main(String[] args) {
        SpringApplication.run(MyAppApplication.class, args);
    }
}
```

---

## ⚙️ application.yml Mẫu

```yaml
spring:
  application:
    name: my-spring-app
  
  datasource:
    url: jdbc:postgresql://localhost:5432/mydb
    username: ${DB_USERNAME:admin}
    password: ${DB_PASSWORD:secret}
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: validate        # production: validate, dev: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080
  servlet:
    context-path: /api

logging:
  level:
    com.example.myapp: DEBUG
    org.hibernate.SQL: DEBUG
```

---

*Tags: #spring/structure #java/core*

